const CartWidget = () => {
    return (
        <button className="cartBtn">
            <img className="cartImg" src={'./images/carrito.svg'} alt='cart-widget'/>
            3
        </button>
    )
}

export default CartWidget